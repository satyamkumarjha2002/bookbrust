'use client';

import {
  Upload,
  User,
  X,
  Loader2,
  type LucideIcon,
} from 'lucide-react';

export type Icon = LucideIcon;

export const Icons = {
  spinner: Loader2,
  user: User,
  upload: Upload,
  close: X,
}; 